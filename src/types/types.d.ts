export type Pokemon = {
    name: string;
    id: string;
    imgSrc: string;
}

export type PokemonDetails = {
    name: string;
    id: string;
    imgSrc: string;
    hp: number;
    attack: string;
    defense: string;
}